GOOGLE_CALENDAR_OAUTH_HINT = (
    "Requires connected Google account (Calendar + Gmail). Check google.auth.status; connect via "
    "google.auth.connect_url or /connect_google. "
)

GOOGLE_GMAIL_OAUTH_HINT = (
    "Requires connected Google account with Gmail scope (google.auth.status gmail_ready=true). "
    "Connect via google.auth.connect_url or /connect_google. "
)

GOOGLE_DRIVE_OAUTH_HINT = (
    "Requires connected Google account with Drive scope (google.auth.status drive_ready=true). "
    "Connect via google.auth.connect_url or /connect_google. "
)

GOOGLE_SHEETS_OAUTH_HINT = (
    "Requires connected Google account with Sheets scope (google.auth.status sheets_ready=true). "
    "Connect via google.auth.connect_url or /connect_google. "
)

GOOGLE_TASKS_OAUTH_HINT = (
    "Requires connected Google account with Tasks scope (google.auth.status tasks_ready=true). "
    "Connect via google.auth.connect_url or /connect_google. "
)
